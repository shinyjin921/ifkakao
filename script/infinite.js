const cardList = document.querySelectorAll('.card-list');

const cardInit = ()=>{
  const slideCard = document.querySelector(".caption");
  const cardImg = document.querySelectorAll(".card-list>li");
  const cardSize = cardImg.length;
}

const total = cardSize + 1 ;
const firstClonCard = cardImg[0].clonenode(true);


